<?php

    require_once __DIR__ . "/vendor/autoload.php";

    use Framework\Console\Input;
    use Dotenv\Dotenv;

    $dotenv = Dotenv::createImmutable(__DIR__);
    $dotenv->load();

    $inputObj = new Input($argv);

?>